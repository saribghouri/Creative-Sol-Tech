/******* Do not edit this file *******
Simple Custom CSS and JS - by Silkypress.com
Saved: Dec 11 2024 | 15:58:49 */
// Footer JS
jQuery(document).ready(function($) {
	setTimeout(function () {
		$('.elementor-location-footer > .elementor-element').removeAttr('style');
	}, 500);
});
// Footer JS


/*-- Typing Animation --*/
  var TxtType = function (el, toRotate, period) {
    this.toRotate = toRotate;
    this.el = el;
    this.loopNum = 0;
    this.period = parseInt(period, 10) || 2000;
    this.txt = '';
    this.tick();
    this.isDeleting = false;
  };
  TxtType.prototype.tick = function () {
    var i = this.loopNum % this.toRotate.length;
    var fullTxt = this.toRotate[i];

    if (this.isDeleting) {
      this.txt = fullTxt.substring(0, this.txt.length - 1);
    } else {
      this.txt = fullTxt.substring(0, this.txt.length + 1);
    }

    this.el.innerHTML = '<span class="wrap">' + this.txt + '<span class="wrap-border"></span></span>';

    var that = this;
    var delta = 200 - Math.random() * 100;

    if (this.isDeleting) {
      delta /= 2;
    }
    if (!this.isDeleting && this.txt === fullTxt) {
      delta = this.period;
      this.isDeleting = true;
    } else if (this.isDeleting && this.txt === '') {
      this.isDeleting = false;
      this.loopNum++;
      delta = 500;
    }

    setTimeout(function () {
      that.tick();
    }, delta);
  };
  window.onload = function () {
	setTimeout(function () { // Add 3-second delay
    var elements = document.getElementsByClassName('typewrite');
    for (var i = 0; i < elements.length; i++) {
      var toRotate = elements[i].getAttribute('data-type');
      var period = elements[i].getAttribute('data-period');
      if (toRotate) {
        new TxtType(elements[i], JSON.parse(toRotate), period);
      }
    }
	}, 1500); // 3000ms = 3 seconds
  };
/*-- Typing Animation --*/



// Email on overlay menu
jQuery(document).ready(function($){
  jQuery('.overlay-menu .right-area ul:nth-child(1) li:nth-child(1):not(.overlay-menu .right-area ul.social-links li:nth-child(1))').append('<li><a href="mailto:sales@360 Austrailiaventures.com" target="_blank">sales@360 Austrailiaventures.com</a></li>');
});
// Email on overlay menu



// Homepage tabs image change onClick
jQuery(document).ready(function ($) {
    // Event listener for Bootstrap tab activation
    $('button[data-bs-toggle="tab"]').on('shown.bs.tab', function () {
        if ($("li.nav-item:nth-child(1) button.nav-link").hasClass("active")) {
            $(".what-is-img img").hide();
            $(".hire-us-img img").fadeIn();
			$(".cust-inn-4steps").show();
        } else if ($("li.nav-item:nth-child(2) button.nav-link").hasClass("active")) {
            $(".hire-us-img img").hide();
            $(".what-is-img img").fadeIn();
			$(".cust-inn-4steps").hide();
        }
    });

    // Initial check for the active class on page load
    if ($("li.nav-item:nth-child(1) button.nav-link").hasClass("active")) {
        $(".what-is-img img").hide();
        $(".hire-us-img img").fadeIn();
		$(".cust-inn-4steps").show();
    } else if ($("li.nav-item:nth-child(2) button.nav-link").hasClass("active")) {
        $(".hire-us-img img").hide();
        $(".what-is-img img").fadeIn();
		$(".cust-inn-4steps").hide();
    }
});
// Homepage tabs image change onClick


// All external urls should open in new tab
jQuery(document).ready(function ($) {
    $('a').each(function () {
        var linkHost = this.hostname; // Get the hostname of the link
        if (linkHost && linkHost !== location.hostname) {
            $(this).attr('target', '_blank'); // Open in a new tab
        }
    });
});
// All external urls should open in new tab